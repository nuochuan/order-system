spring:
  application:
    name: ruyuan-eshop-order
  profiles:
    active: local
  cloud:
    nacos:
      config:
        server-addr: localhost:8848
        file-extension: yaml
        username: nacos
        password: nacos