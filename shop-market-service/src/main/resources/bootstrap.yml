spring:
  application:
    name: shop-market
  profiles:
    active: dev
  cloud:
    nacos:
      config:
        shared-configs:
          - data-id: shop-common-${spring.profiles.active}.yaml
            group: DEFAULT_GROUP
            refresh: true
        server-addr: localhost:8848
        file-extension: yaml